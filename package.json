{
  "name": "bun-mc-launcher",
  "version": "1.0.0",
  "module": "src/index.ts",
  "private": false,
  "type": "module",
  "scripts": {
    "dev": "bun .",
    "build": "rm -rf dist && bun build --target=bun --minify --sourcemap=none --outdir=dist ./src/index.ts",
    "start": "bun run dist/index.js",
    "compile": "bun build --compile --minify --sourcemap --bytecode --bytecode . --outfile dist/myapp",
    "test": "bun test",
    "format": "biome format --write ./src",
    "lint": "biome lint ./src",
    "check": "biome check ./src"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/adm-zip": "^0.5.7",
    "@types/bun": "^1.2.8"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "adm-zip": "^0.5.16"
  }
}
